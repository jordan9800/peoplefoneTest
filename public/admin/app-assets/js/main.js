$('#countdown').countdown({
	date: "January 26, 2019 18:03:26",
	render: function(data) {
	  var el = $(this.el);
	  el.empty()
	    .append("<div> <span class='pdRight15'>Time left:</span> </div>")
	    .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
	    .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
	    .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
	    .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
	}
});

$('#countdown1').countdown({
    date: "January 27, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown2').countdown({
    date: "January 28, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown3').countdown({
    date: "January 29, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown4').countdown({
    date: "January 30, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown5').countdown({
    date: "January 31, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown6').countdown({
    date: "January 26, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown7').countdown({
    date: "January 26, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown8').countdown({
    date: "January 20, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown9').countdown({
    date: "January 25, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown10').countdown({
    date: "January 24, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown11').countdown({
    date: "January 23, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown12').countdown({
    date: "January 22, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown13').countdown({
    date: "January 21, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown14').countdown({
    date: "January 18, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown15').countdown({
    date: "January 19, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='pdRight15'>Time left:</span> </div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.min, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timeLeft'> <span>" + this.leadingZeros(data.sec, 2) + "</span></div>");
    }
});

$('#countdown_1').countdown({
    date: "February 15, 2019 18:03:26",
    render: function(data) {
      var el = $(this.el);
      el.empty()
        .append("<div> <span class='timeCount'>Time left:</span> </div>")
        .append("<div class='timerOn'> <span>" + this.leadingZeros(data.days, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timerOn'> <span>" + this.leadingZeros(data.hours, 2) + "</span><span class='clrBlack'>:</span></div>")
        .append("<div class='timerOn'> <span>" + this.leadingZeros(data.min, 2) + "</span></div>")
    }
});


(function ($) {
    "use strict";
    /*==================================================================
    [ Simple slide100 ]*/

    $('.simpleslide100').each(function(){
        var delay = 7000;
        var speed = 1000;
        var itemSlide = $(this).find('.simpleslide100-item');
        var nowSlide = 0;

        $(itemSlide).hide();
        $(itemSlide[nowSlide]).show();
        nowSlide++;
        if(nowSlide >= itemSlide.length) {nowSlide = 0;}

        setInterval(function(){
            $(itemSlide).fadeOut(speed);
            $(itemSlide[nowSlide]).fadeIn(speed);
            nowSlide++;
            if(nowSlide >= itemSlide.length) {nowSlide = 0;}
        },delay);
    });


})(jQuery);